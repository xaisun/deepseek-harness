## DeepSeek Harness 桌面版 v1.0.1

Windows 桌面客户端预编译发布包（约 256MB）。

### 安装与使用
1. 下载 `DeepSeek Harness v1.0.1.zip` 并解压到任意目录。
2. 双击 `DeepSeek Harness.exe` 启动。
3. 运行时已打包在 `harness-runtime/`，无需额外安装 Node.js。

### 包含内容
- `DeepSeek Harness.exe` — 主程序
- `harness-runtime/` — 运行时与 dsh 组件
- `dsh-data/` — 用户数据目录
- `dsh.ico` — 图标

### 校验
- 发布日期：2026-08-16
- 版本：v1.0.1

### 修复与增强（补丁）
- **新增「重启前端」入口**：☰ 菜单与系统托盘右键菜单均新增「重启前端」。修改对话窗口 UI（如放大窗口、TOKEN 一行显示、显示会话费用）后，点击即可让改动生效，**无需彻底退出重开**桌面版。
- **根因**：web 服务（`:3080`）在启动时一次性读入 `client.js`，因此 UI 改动必须重启服务才能生效；此前只能退出桌面版重开，表现为"改完窗口就不见"。现提供热重启入口解决该问题。
